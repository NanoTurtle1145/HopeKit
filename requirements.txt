# HopeKit 支持两套 Qt6 绑定，二选一即可：
#
# 推荐（优先）：PySide6 — Qt 官方维护，LGPL 授权（闭源商用免费）
#   pip install PySide6 PySide6-Addons
#
# 兼容：PyQt6 — 社区维护，GPL/商业双授权
#   pip install PyQt6 PyQt6-WebEngine
#
# 程序启动时自动检测，优先用 PySide6，回退到 PyQt6。

# 默认装 PySide6（推荐）
PySide6>=6.5
