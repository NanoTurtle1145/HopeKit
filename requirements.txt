# HopeKit 支持两套 Qt 绑定，二选一即可：
#
# 推荐（优先）：PySide6 — Qt 官方维护，LGPL 授权（闭源商用免费）
#   pip install PySide6 PySide6-Addons
#
# 兼容（旧版）：PyQt5 — 已停止功能更新，GPL/商业双授权
#   pip install PyQt5>=5.15 PyQtWebEngine>=5.15
#
# 程序启动时自动检测，优先用 PySide6，回退到 PyQt5。

# 默认装 PySide6（2026 年推荐）
PySide6>=6.5
